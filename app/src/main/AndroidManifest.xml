<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vikjo.openmicevent1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BusinessViewParticipants"></activity>
        <activity android:name=".ArtistMyEvents" />
        <activity android:name=".CreateEventActivity" />
        <activity android:name=".ArtistHome" />
        <activity android:name=".BusinessHome" />
        <activity android:name=".BusinessRegisterActivity" />
        <activity android:name=".ArtistRegisterActivity" />
        <activity android:name=".RegisterAsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>